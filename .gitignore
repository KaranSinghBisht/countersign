node_modules/
dist/
coverage/
*.tsbuildinfo

# Secrets. Never commit these.
.env
.env.*
!.env.example
*.pem
*.key
keys/

# Generated per-developer by `make setup`. Holds only public keys, but
# committing one would suggest the verifier should trust these particular
# keys — and the verifier's trust must be established out of band.
trust.json

# Local artifacts
.countersign/
export-*.tar.gz
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json
.omc/
